# Build artifacts
/bin/
/dist/
*.exe
*.out

# Embedded Vue build output — regenerated by `make web` / `npm run build`.
# Must exist at `go build` time, but it's a build artifact, not source.
/web/dist/

# Frontend deps
/web/node_modules/

# Go
*.test
*.prof
coverage.out

# Editor / OS
.DS_Store
.idea/
.vscode/
*.swp
